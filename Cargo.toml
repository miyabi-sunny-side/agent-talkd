[package]
name = "agent-talkd"
version = "0.14.4"
edition = "2024"
rust-version = "1.96"
description = "A remote message console for Herdr coding sessions"
license = "MIT"
repository = "https://github.com/miyabi-sunny-side/agent-talkd"
readme = "README.md"
keywords = ["herdr", "cli", "agent", "claude", "codex"]
categories = ["command-line-utilities", "development-tools"]
publish = false
build = "build.rs"

[[bin]]
name = "agent-talk"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
bytes = "1.11"
flate2 = "1.1"
http-body-util = "0.1"
hyper = { version = "1.8", features = ["http1", "server"] }
hyper-util = { version = "0.1", features = ["tokio"] }
libc = "0.2"
semver = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
tar = "0.4"
tempfile = "3.27"
tokio = { version = "1.49", features = ["fs", "io-std", "io-util", "macros", "net", "process", "rt-multi-thread", "signal", "sync", "time"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

[dev-dependencies]
# Isolated socket tests can control time without delaying the suite.
tokio = { version = "1.49", features = ["test-util"] }

[lints.clippy]
all = "warn"
pedantic = "warn"

[profile.release]
opt-level = 3
lto = false
strip = true
codegen-units = 16
